import { PlayerRankPipe } from './player-rank.pipe';

describe('PlayerRankPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerRankPipe();
    expect(pipe).toBeTruthy();
  });
});
