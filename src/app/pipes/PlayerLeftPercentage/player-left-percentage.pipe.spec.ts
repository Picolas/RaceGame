import { PlayerLeftPercentagePipe } from './player-left-percentage.pipe';

describe('PlayerLeftPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerLeftPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
