import { TeamLeftPercentagePipe } from './team-left-percentage.pipe';

describe('TeamLeftPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new TeamLeftPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
